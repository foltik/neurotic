neuro_preload(() => {
    //neuro_source('file', {path: 'candyland.mp3'});
    //neuro_source('stream', {url: 'https://radio.xyzzy.link/wizard'});
});

neuro_setup(() => {
    //neuro_source('in');
    let canvas = createCanvas(500, 500);
});

neuro_draw(() => {
    background(0);
});
